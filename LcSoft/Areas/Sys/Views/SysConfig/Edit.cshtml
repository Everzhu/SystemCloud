@model XkSystem.Areas.Sys.Models.SysConfig.Edit
@{
    ViewBag.TopBar = "Hidden";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <nav class="navbar navbar-default mybar">
        <div class="navbar-right">
            <a href="@Url.Action("List")" class="btn btn-default">
                <span class="glyphicon glyphicon-chevron-left"></span>
                @Resources.LocalizedText.BtnReturn
            </a>
        </div>
    </nav>
    <div class="container-fluid">
        <div class="form-horizontal">
            @Html.HiddenFor(d => d.SysConfigEdit.Id)
            <div class="form-group">
                <label class="col-md-2 control-label">
                    @Html.LabelForRequired(d => d.SysConfigEdit.Title)
                </label>
                <div class="col-md-10">
                    @Html.TextBoxFor(d => d.SysConfigEdit.Title, new { @class = "form-control" })
                    @Html.ValidationMessageFor(d => d.SysConfigEdit.Title)
                </div>
            </div>
            <div class="form-group">
                <label class="col-md-2 control-label">
                    @Html.LabelForRequired(d => d.SysConfigEdit.Value)
                </label>
                <div class="col-md-10">
                    @Html.TextBoxFor(d => d.SysConfigEdit.Value, new { @class = "form-control" })
                    @Html.ValidationMessageFor(d => d.SysConfigEdit.Value)
                </div>
            </div>
            <div class="form-group">
                <div class="col-md-12">
                    <button type="submit" class="btn btn-primary">
                        <span class="glyphicon glyphicon-ok"></span>
                        @Resources.LocalizedText.BtnOk
                    </button>
                </div>
            </div>
        </div>
    </div>
}