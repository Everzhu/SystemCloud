@model XkSystem.Areas.Quality.Models.QualityPhoto.Edit
@{
    ViewBag.TopBar = "Hidden";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="container-fluid">
        <div class="form-horizontal">
            @Html.HiddenFor(d => d.QualityPhotoEdit.Id)
            <div class="form-group">
                <label class="col-xs-3 control-label">
                    @Html.LabelForRequired(d => d.QualityPhotoEdit.PhotoTitle)
                </label>
                <div class="col-xs-9">
                    @Html.TextBoxFor(d => d.QualityPhotoEdit.PhotoTitle, new { @class = "form-control" })
                    @Html.ValidationMessageFor(d => d.QualityPhotoEdit.PhotoTitle)
                </div>
            </div>
            <div class="form-group hidden">
                <div class="col-md-12">
                    <button type="submit" class="btn btn-primary">
                        <span class="glyphicon glyphicon-ok"></span>
                        @Resources.LocalizedText.BtnOk
                    </button>
                </div>
            </div>
        </div>
    </div>
}