@model XkSystem.Areas.Basis.Models.ClassAllotResult.Edit
@{
    ViewBag.TopBar = "Hidden";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="container-fluid">
        <div class="form-horizontal">
            @Html.HiddenFor(d => d.ClassAllotResultEdit.Id)
            <div class="form-group">
                <label class="col-xs-3 control-label">
                    @Html.LabelForRequired(d => d.ClassAllotResultEdit.ClassAllotClassId)
                </label>
                <div class="col-xs-9">
                    @Html.DropDownListFor(d => d.ClassAllotResultEdit.ClassAllotClassId, Model.ClassAllotClassList, new { @class = "form-control" })
                    @Html.ValidationMessageFor(d => d.ClassAllotResultEdit.ClassAllotClassId)
                </div>
            </div>
            <div class="form-group hidden">
                <div class="col-xs-12">
                    <button type="submit" class="btn btn-primary">
                        <span class="glyphicon glyphicon-ok"></span>
                        @Resources.LocalizedText.BtnOk
                    </button>
                </div>
            </div>
        </div>
    </div>
}