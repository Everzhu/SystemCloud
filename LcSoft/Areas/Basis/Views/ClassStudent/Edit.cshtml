@model XkSystem.Areas.Basis.Models.ClassStudent.Edit
@{
    ViewBag.TopBar = "Hidden";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="container-fluid">
        <div class="form-horizontal">
            @Html.HiddenFor(d => d.ClassStudentEdit.Id)
            <div class="form-group">
                <label class="col-xs-3 control-label">
                    @Html.LabelForRequired(d => d.ClassStudentEdit.No)
                </label>
                <div class="col-xs-9">
                    @Html.TextBoxFor(d => d.ClassStudentEdit.No, new { @class = "form-control" })
                    @Html.ValidationMessageFor(d => d.ClassStudentEdit.No)
                </div>
            </div>
            <div class="form-group hidden">
                <div class="col-xs-12">
                    <button type="submit" class="btn btn-primary">
                        <span class="glyphicon glyphicon-ok"></span>
                        @Resources.LocalizedText.BtnOk
                    </button>
                </div>
            </div>
        </div>
    </div>
}