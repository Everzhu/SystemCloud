@model XkSystem.Areas.Dict.Models.DictHealth.Edit
@{
    ViewBag.TopBar = "Hidden";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="container-fluid">
        <div class="form-horizontal">
            @Html.HiddenFor(d => d.HealthEdit.Id)
            <div class="form-group">
                <label class="col-xs-3 control-label">
                    @Html.LabelForRequired(d => d.HealthEdit.No)
                </label>
                <div class="col-xs-9">
                    @Html.TextBoxFor(d => d.HealthEdit.No, new { @class = "form-control", placeholder = "自动排序编号" })
                    @Html.ValidationMessageFor(d => d.HealthEdit.No)
                </div>
            </div>
            <div class="form-group">
                <label class="col-xs-3 control-label">
                    @Html.LabelForRequired(d => d.HealthEdit.HealthName)
                </label>
                <div class="col-xs-9">
                    @Html.TextBoxFor(d => d.HealthEdit.HealthName, new { @class = "form-control" })
                    @Html.ValidationMessageFor(d => d.HealthEdit.HealthName)
                </div>
            </div>
            <div class="form-group hidden">
                <div class="col-xs-12">
                    <button type="submit" class="btn btn-primary">
                        <span class="glyphicon glyphicon-ok"></span>
                        @Resources.LocalizedText.BtnOk
                    </button>
                </div>
            </div>
        </div>
    </div>
}