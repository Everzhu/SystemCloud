@model XkSystem.Areas.Perform.Models.PerformComment.Edit
@{
    ViewBag.TopBar = "Hidden";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <nav class="navbar navbar-default mybar">
        <div class="navbar-right">
            <a href="@Url.Action("List")" class="btn btn-default">
                <span class="glyphicon glyphicon-chevron-left"></span>
                @Resources.LocalizedText.BtnReturn
            </a>
        </div>
    </nav>
    <div class="container-fluid">
        <div class="form-horizontal">
            @Html.HiddenFor(d => d.PerformCommentEdit.Id)
            <div class="form-group">
                <label class="col-md-2 control-label">
                    @Html.LabelForRequired(d => d.PerformCommentEdit.Comment)
                </label>
                <div class="col-md-10">
                    @Html.TextBoxFor(d => d.PerformCommentEdit.Comment, new { @class = "form-control" })
                    @Html.ValidationMessageFor(d => d.PerformCommentEdit.Comment)
                </div>
            </div>
            <div class="form-group">
                <div class="col-md-12">
                    <button type="submit" class="btn btn-primary">
                        <span class="glyphicon glyphicon-ok"></span>
                        @Resources.LocalizedText.BtnOk
                    </button>
                    <a href="@Url.Action("List")" class="btn btn-default">
                        <span class="glyphicon glyphicon-remove"></span>
                        @Resources.LocalizedText.BtnCancel
                    </a>
                </div>
            </div>
        </div>
    </div>
}