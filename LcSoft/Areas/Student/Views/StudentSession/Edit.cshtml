@model XkSystem.Areas.Student.Models.StudentSession.Edit
@{
    ViewBag.TopBar = "Hidden";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="container-fluid">
        <div class="form-horizontal">
            @Html.HiddenFor(d => d.StudentSessionEdit.Id)
            <div class="form-group">
                <label class="col-xs-3 control-label">
                    @Html.LabelForRequired(d => d.StudentSessionEdit.No)
                </label>
                <div class="col-xs-9">
                    @Html.TextBoxFor(d => d.StudentSessionEdit.No, new { @class = "form-control", placeholder = "自动排序编号" })
                    @Html.ValidationMessageFor(d => d.StudentSessionEdit.No)
                </div>
            </div>
            <div class="form-group">
                <label class="col-xs-3 control-label">
                    @Html.LabelForRequired(d => d.StudentSessionEdit.StudentSessionName)
                </label>
                <div class="col-xs-9">
                    @Html.TextBoxFor(d => d.StudentSessionEdit.StudentSessionName, new { @class = "form-control" })
                    @Html.ValidationMessageFor(d => d.StudentSessionEdit.StudentSessionName)
                </div>
            </div>
            <div class="form-group hidden">
                <div class="col-xs-12">
                    <button type="submit" class="btn btn-primary">
                        <span class="glyphicon glyphicon-ok"></span>
                        @Resources.LocalizedText.BtnOk
                    </button>
                </div>
            </div>
        </div>
    </div>
}