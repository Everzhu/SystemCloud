@model XkSystem.Areas.Study.Models.StudyOption.List

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken();
    <h4>晚自习参数</h4>
    <nav class="navbar">
        <div class="navbar-right">
            <a href="@Url.Action("Edit")" class="btn btn-primary pop" dataHeight="200px">
                <span class="glyphicon glyphicon-plus"></span>
                @Resources.LocalizedText.BtnAdd
            </a>
            <a href="@Url.Action("Delete")" class="btn btn-danger delete">
                <span class="glyphicon glyphicon-trash"></span>
                @Resources.LocalizedText.BtnDelete
            </a>
        </div>
        <div class="navbar-left navbar-form">
            <div class="btn-group form-group">
                <a class="btn btn-warning active" href="@Url.Action("List", "StudyOption")">晚自习表现</a>
            </div>
            <div class="input-group form-group">
                @Html.TextBoxFor(d => d.SearchText, new { @class = "form-control", placeholder = Resources.LocalizedText.TipSearch })
                <span class="input-group-btn">
                    <button class="btn btn-primary" type="submit">
                        <span class="glyphicon glyphicon-search"></span>
                    </button>
                </span>
            </div>
        </div>
    </nav>
    <div class="table-responsive">
        <table class="table table-bordered table-hover table-striped">
            <thead>
                <tr>
                    <th style="width:30px">
                        @Html.CheckBox("CboxAll")
                    </th>
                    <th style="width:80px">
                        @Html.DisplayNameFor(d => d.StudyOptionList.FirstOrDefault().No)
                    </th>
                    <th>
                        @Html.DisplayNameFor(d => d.StudyOptionList.FirstOrDefault().StudyOptionName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(d => d.StudyOptionList.FirstOrDefault().StudyOptionValue)
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var a in Model.StudyOptionList)
                {
                    <tr>
                        <td>
                            <input type="checkbox" id="CboxId" name="CboxId" value="@a.Id" />
                        </td>
                        <td>
                            @a.No
                        </td>
                        <td class="text-left">
                            <a href="@Url.Action("Edit", new { id = a.Id })" class="pop" dataHeight="200px">@a.StudyOptionName</a>
                        </td>
                        <td class="text-left">
                            <a href="@Url.Action("Edit", new { id = a.Id })" class="pop" dataHeight="200px">@a.StudyOptionValue</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}