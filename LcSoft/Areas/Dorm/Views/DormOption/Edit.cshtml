@model XkSystem.Areas.Dorm.Models.DormOption.Edit
@{
    ViewBag.TopBar = "Hidden";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="container-fluid">
        <div class="form-horizontal">
            @Html.HiddenFor(d => d.DormOptionEdit.Id)
            <div class="form-group">
                <label class="col-xs-3 control-label">
                    @Html.LabelForRequired(d => d.DormOptionEdit.No)
                </label>
                <div class="col-xs-9">
                    @Html.TextBoxFor(d => d.DormOptionEdit.No, new { @class = "form-control", placeholder = "自动排序编号" })
                    @Html.ValidationMessageFor(d => d.DormOptionEdit.No)
                </div>
            </div>
            <div class="form-group">
                <label class="col-xs-3 control-label">
                    @Html.LabelForRequired(d => d.DormOptionEdit.DormOptionName)
                </label>
                <div class="col-xs-9">
                    @Html.TextBoxFor(d => d.DormOptionEdit.DormOptionName, new { @class = "form-control" })
                    @Html.ValidationMessageFor(d => d.DormOptionEdit.DormOptionName)
                </div>
            </div>
            <div class="form-group">
                <label class="col-xs-3 control-label">
                    @Html.LabelForRequired(d => d.DormOptionEdit.DormOptionValue)
                </label>
                <div class="col-xs-9">
                    @Html.TextBoxFor(d => d.DormOptionEdit.DormOptionValue, new { @class = "form-control" })
                    @Html.ValidationMessageFor(d => d.DormOptionEdit.DormOptionValue)
                </div>
            </div>
            <div class="form-group hidden">
                <div class="col-xs-12">
                    <button type="submit" class="btn btn-primary">
                        <span class="glyphicon glyphicon-ok"></span>
                        @Resources.LocalizedText.BtnOk
                    </button>
                </div>
            </div>
        </div>
    </div>
}

